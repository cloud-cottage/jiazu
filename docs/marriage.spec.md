# 跨树嫁娶（嫁出 / 娶入 / 绝婚 / 合离）— 规格（marriage.spec.md）

> 状态：**设计已确认（Kevin 2026-09）· 已实施（2026-09-19）** —— 口径 A（镜像节点「点开即真身」）**真机质检通过**、发现通道（§9-8）**冒烟 / 单测通过**、首页卡片人数单列镜像（§11-4）**冒烟通过**、共享树选择器展示层（**§9-9**）与 H5 遮罩关闭修复（**§9-10**，跨模块通用坑）**真机质检通过**（上云动作见 `docs/PENDING_DEPLOY.md` **§22**）；§11 挂账项（镜像候选、写侧镜像 handle 拦截等）**仍未修**，逐条见 §11。
> 真源级别：规格文件。实施（compat-api / frontend）必须与本文件一致；与 `docs/data-model.md`、`docs/permission-tier.spec.md` 冲突时以本文件为准并同步修订彼文件。
> 关联：README「树内操作」、`cloudfunctions/compat-api/lib/tree-write.js`（`spouseSlots` / `addSpouseNode`）、`docs/data-model.md`（集合与 API 契约）。

## 1. 已确认口径（Kevin）

| # | 口径 | 对设计的影响 |
|---|---|---|
| 1 | **添加配偶一般来自"树外"其他家族树**，同树非常罕见 | 「嫁出 / 娶入」是**主路径**；同树婚配（现有 `/admin/add-spouse`）保留为罕见分支 |
| 2 | 「出嫁」必须有**反向的「娶入」** | 同一动作两个方向（`direction: out / in`），不写两套逻辑 |
| 3 | **允许再嫁、再娶**，加标识表明是第几次 | 不做互斥；用**婚姻序号**（初嫁/再嫁·初娶/再娶） |
| 4 | 发起人对**本人树**必须有写权；**目标树权限** | **取方式 C：申请制** —— 发起 → 对方树审批 → 生效（§8） |
| 5 | 解除婚姻拆成**「绝婚」（男方主动）/「合离」（双方自愿）** 两种动作 | 两个动作、两套同意规则（§5） |
| 6 | 嫁出 / 娶入 / 绝婚 / 合离 **四种动作都可选填年份或日期** | 日期为**选填**；格式与既有生卒一致（`1957` 或 `1957-12-04`，走 `dateDisplay()` 规范化；§4） |
| 7 | 其余按草案建议 | 镜像节点单列计数；GEDCOM 用 `_MIRROR` 标注不导出为真人；再嫁/再娶允许并存 |

## 2. 核心原则与术语

- **结构事实只有一个来源：`families`**（父槽 / 母槽 / `child_handles`）。`external_*` 只承载**跨树指针**。
  现状相反（出嫁只写指针、不建家族）→ 本规格把两侧都补成结构完整。
- **镜像节点（mirror）**：在对方树里代表本人的节点（`external_mirror='true'` + `external_person_handle` 指真身 + `external_tree`=真身所在树，**三条齐备**，见 §9-7-1；`external_link_type` 可为 `marriage` / `child` / `founder` / `chain`，角标分档见 §9-7-6）。**树内保持可见、点开即真身**（§9-7）。项目已有同类模式（zhonghua 跨树登记标记），不引入新概念。
- **配对 id**：`external_marriage_id`（一段婚姻两侧同值，用于成对维护/解除）。
- **婚姻序号**：`external_marriage_no`（本人侧 1,2,3…；1=初嫁/初娶，≥2=再嫁/再娶）。
- **真身不动**：本人始终留在原树，不搬迁节点。

## 3. 字段（结构层属性，加入 `EXTERNAL_KEYS`）

| 字段 | 写入侧 | 含义 |
|---|---|---|
| `external_tree` / `external_person_handle` / `external_link_type='marriage'` | 两侧 | 跨树指针（沿用既有三字段） |
| `external_marriage_id` | 两侧 | 配对 id（同值） |
| `external_marriage_no` | 两侧（各自序号） | 本人侧第几次婚姻（1=初嫁/初娶） |
| `external_marriage_date` | 两侧 | **选填**：成婚年份/日期（`1957` / `1957-12-04`） |
| `external_marriage_end_kind` | 两侧 | 结束方式：`绝婚` / `合离` |
| `external_marriage_end_date` | 两侧 | **选填**：结束年份/日期 |
| `external_marriage_end_by` | 两侧 | 结束发起方（`husband_side` / `mutual`），用于展示与追溯 |
| `external_marriage_created_by` | 两侧 | 发起人 phone（留痕，权限 C 的兜底手段） |
| `external_relation_note` | 两侧 | 人类可读说明（如「风华胥 嫁至 ji_23395_01 的 季某」） |

## 4. 日期规则

- 四个动作（嫁出/娶入/绝婚/合离）的日期字段**均为选填**，留空即不写该属性。
- 允许 `YYYY`（如 `1957`）或 `YYYY-MM-DD`（如 `1957-12-04`）；服务端用与生卒相同的规范化（`dateDisplay()` 同规则），非法格式 → 400「日期格式需为 1957 或 1957-12-04」。
- 展示：档案婚姻块显示「成婚 1957 · 绝婚 1960」；未填则不显示该段。

## 5. 动作定义与写入矩阵

以 `P`（本人，树 `S`）与 `Q`（配偶，树 `T`）为例，`mid` = 新配对 id，`no` = P 侧序号。

| | 嫁出（`out`，P 为女，T 为夫家） | 娶入（`in`，P 为男，T 为妻家） |
|---|---|---|
| S 侧家族 | 建/补家族：**P 入本人性别槽**，另一槽放 `Q` 镜像 | 同左（P 入父槽） |
| S 侧镜像 | `M_Q`：姓名/性别取自 Q、指向 Q、`link_type='marriage'`、`marriage_id=mid` | 同左 |
| S 侧 P | `spouse_families += 家族`；写 §3 全部指针字段（含序号、可选日期） | 同左 |
| T 侧家族 | 在 Q 的家族里把 **P 的镜像 `M_P`** 放入 Q 的另一槽（无家族则新建） | 同左 |
| T 侧 Q | 写 §3 指针字段（含 Q 侧自己的序号、可选日期） | 同左 |
| 子女 | **归父（真实节点）所在树**：父为真人、母为镜像 → 子女建在父的真树家族下；母树侧仅显示镜像子女。反向（招赘：父为镜像）按同原则归真人一侧 | 同左 |

### 结束动作（两个）

| 动作 | 发起方 | 同意规则 | 效果 |
|---|---|---|---|
| **绝婚** | **男方所在树**（单方主动） | **无需对方同意**（男方主动解除的语义），生效后通知对方树 | 删两侧家族槽 + 两侧镜像 + 清两侧婚姻指针；家族空则删除；写 `end_kind='绝婚'`、`end_date`(选填)、`end_by='husband_side'` |
| **合离** | 任一侧 | **需对方树确认**（申请 → 对方审批通过才生效；对方拒绝则保持婚姻） | 同上，`end_kind='合离'`、`end_by='mutual'` |

- **历史留痕**：结束后两侧 person 上保留 `external_marriage_end_kind/_end_date`（用于"再嫁/再娶"叙事与档案展示），但**清除** `external_marriage_id`、镜像节点与家族槽位（视为关系终止，结构上回到单身态，允许再嫁/再娶）。
- **幂等**：已结束的婚姻再次结束 → 200 + `already_ended`。

## 6. 规则与不变量

1. **再嫁/再娶**：允许并存多段；`no` 递增；不自动解除前段（产品上提示"如已改嫁，建议先绝婚/合离"，不强制）。
2. **不可重复配对**：同一对真身之间已存在**有效**（未结束）婚姻 → 400「二人已建立婚姻关系」。
3. **镜像成对**：镜像节点两侧同 `mid` 成对；任一侧删除/结束 → 另一侧同步清理（服务端强制）。
4. **不可指向自己/同树**：`P` 与 `Q` 必须不同树（同树走 §7）。
5. **性别与方向**：`out` 仅女（P 为女）、`in` 仅男（P 为男）；性别未知（U）需显式指定方向，否则 400。
6. **镜像节点**：界面必须带「外树」标（角标按 `external_link_type` 分档，§9-7-6）；不计入该树"真人"叙事（计数与统计栏按档分列，§9-7-7 / §9-4）。

## 7. 同树婚配（罕见路径）

保留 `/admin/add-spouse`（新建本树节点 / 挂接本树已有节点），补写 `external_marriage_no`（同树也计数）与可选 `external_marriage_date`，便于档案统一显示「初嫁/再嫁」。

### 7-4 跨树婚姻的子女归属（2026-09-15 新增）

在「跨树婚姻家庭」下执行「添加子女」时，若父为该树的外树**镜像节点**（真身在对方树），
子女**必须建在父的真身所在树**，挂到父的真实家族槽位；母树侧只保留镜像子女用于显示。

- 实测纠偏：shen 树曾把 季清昆 建在「父 季志全(镜像) / 母 沈伟(真人)」家庭下 →
  已迁移为 ji 树的 `I500059 季清昆`（父家族 = 季志全/沈伟 的真实家族），shen 侧移除。
- 实现要点：「添加子女」需判断所选家庭的父/母是否为镜像（`external_mirror`），
  是则经 `updateTrees()` 把写入路由到真身所在树；未实现前，跨树婚姻家庭的子女入口应给出提示或禁用。

## 8. 权限（方式 C：申请制）

- **发起人**：对**本人树**需写权（`requireWriteUser`：tree_steward / chief_editor；总谱仅 chief）。
- **目标树**：**不要求发起人具备目标树写权**；跨树写入通过"申请 → **目标树审批**"完成，审批人需**目标树**的写权（steward / chief；总谱仅 chief）。
- **申请单**：集合 `jiazu_marriage_requests`（本地模式落在 `migrate-output/collections/jiazu_marriage_requests.json`）：
  `{id, action: 'marry'|'divorce', direction: 'out'|'in', kind?: '合离', from_tree, from_person_handle, to_tree, to_person_handle, marriage_id, marriage_date?, end_date?, note, status: 'pending'|'approved'|'rejected', requested_by, created_at, decided_by, decided_at, reject_reason?}`
- **路由**：
  | 路由 | 说明 |
  |---|---|
  | `POST /marriage-request` | 发起（嫁出/娶入/合离）：生成 pending 申请单；返回 `{request_id}` |
  | `POST /admin/approve-marriage` | 目标树审批通过 → **同一事务内**应用双侧写入（§5） |
  | `POST /admin/reject-marriage` | 目标树驳回（同树驳回，不需要对方） |
  | `GET /admin/marriage-requests` | 审批列表（steward 只看自己树相关；chief 全部） |
  | `POST /admin/marry-end` | **绝婚**：男方侧单方生效（需男方树写权），无需申请单；写 `end_kind='绝婚'` |
  | `POST /admin/marry-dissolve` | 兼容旧名 → 内部等同"合离申请"（发起） |
- 留痕：两侧写 `external_marriage_created_by` / `end_by`；申请单保留 `requested_by` / `decided_by`。

## 9. 与既有实现的差距（实施必改）

| # | 现状 | 问题 | 改法 |
|---|---|---|---|
| 1 | 出嫁只写 4 个 `external_*`、不建家族（`person-archive.vue:864-872`） | 本树档案「配偶与子女」区整块消失（`v-if="spouseFamilies.length"`）、卡片无「配XX」；夫家侧零记录 | 按 §5 双侧写入（经申请审批） |
| 2 | `canMarryOut` 只看"女性+可编辑+非总谱" | 无法表达次数，也无"娶入" | 改为「嫁出 / 娶入 / 绝婚 / 合离」入口 + 序号显示 |
| 3 | 树图 marker 过滤 `external_tree && !== treeId` → `pruneMarkers()` **连子树一起删** | 新设计下真身带外树指针 → 总谱树图会把她**连同后代整支隐藏**；且与口径 A「镜像节点在树内保持可见」（§9-7-1）直接冲突 | 镜像判据收紧为 §9-7-1 **三条齐备**（不得用「有 `external_*`」当判据）；镜像节点**不再进 prune 集合、树内保持可见**（角标按 §9-7-6、点击按 §9-7-2 / §9-7-3）；隐藏语义只保留给**非镜像的外树登记标记**（`external_link_type === 'founder'` 的登记标记）。**已实施 + 真机质检通过（2026-09-19）** |
| 4 | 人数统计 `共 N 人`（`person_count` = `Object.keys(tree.people).length`，**含镜像**） | 镜像节点会被计入 | 树图统计栏**按档分列**、只显示大于 0 的档（§9-7-7），形如 `外树子女 2 · 外树始祖 1`（**按当前可见森林里实际绘制出的镜像节点**分档，`外树配偶` 档可为 0，属正常，见 §9-7「统计栏口径澄清」）；`共 N 人` 保留原计入口径（仍含镜像）。首页卡片的「N 人（含外树 M）」另见 §11-4（**已实施 + 冒烟 2026-09-19**）。**已实施 + 真机质检通过（2026-09-19）**（统计栏分档实测见 §9-7「真机质检证据」）（**已作废 2026-09-19**：卡片不再单列镜像，人数口径见 docs/home-sort-search.spec.md §10-6；`mirror_count` 退为保留字段、无消费者） |
| 5 | GEDCOM 导出 | 镜像节点会被导出成真人 | 镜像节点打 `_MIRROR` 标注/跳过 |
| 6 | 跨树写入 | 现有 `updateTree` 只写单树 | 新增跨树事务写入（两棵树顺序写 + 失败回滚/一致性补齐） |

### 9-7 镜像节点口径 A（Kevin 2026-09-19 拍板）：树内保持可见 · 点开即真身

**镜像判据（三条齐备才算镜像）**：① `external_mirror === 'true'` ② `external_person_handle` 非空 ③ `external_tree` 非空。

> ⚠️ **不得用「有 `external_*`」当判据**：**真身自身也带 `external_*` 配偶登记指针** ——
> `ji_23395_01` 的 `I000254` 季清昆（`external_tree='gu_39038_01'`）与 `gu_39038_01` 的 `I000140` 顾景月（`external_tree='ji_23395_01'`）**都是真身**，二者均无 `external_mirror`。

**九条口径（Kevin 2026-09-19 选定，逐条照抄、不得增删数值）**：

| # | 口径 |
|---|---|
| 1 | 判据三条齐备；真身自身带 `external_*` 配偶登记指针，不得用「有 `external_*`」当判据。 |
| 2 | 点镜像 → 打开真身档案（`treeId = external_tree`，`handle = external_person_handle`）。 |
| 3 | 档案顶部标注：本节点为 (真身树 `display_title`) (真身全局编号) 的镜像 · 内容取自真身；树名取 `tree-meta` `display_title`，取不到回退 `tree_id`。 |
| 4 | 编辑与谱系管理按真身树权限判定。 |
| 5 | 真身不可见 / 拉取失败 → 回退镜像本树副本并提示「真身内容不可见」。 |
| 6 | 树图卡片角标按 `external_link_type` 分档：`marriage` 外树配偶 / `child` 外树子女 / `founder` 外树始祖 / `chain` 外树上层链 / 其它 外树登记。 |
| 7 | 树图统计栏由「含外树配偶 M」改为按档分列、只显示大于 0 的档（形如 `外树子女 2 · 外树始祖 1`）；分档统计**按当前可见森林里实际绘制出的镜像节点**计算（`外树配偶` 档可为 0，属正常，见下「统计栏口径澄清」）。（**已作废 2026-09-19**：卡片不再单列镜像，人数口径见 docs/home-sort-search.spec.md §10-6；`mirror_count` 退为保留字段、无消费者） |
| 8 | 红线：管理选人 / 审批选人列表必须保留镜像本身，不做解析（写路径依赖镜像 handle）。 |
| 9 | `external_link_type='branch'` 分迁占位维持跳转新树。 |

**与既有读路径的分工（两条并存、互不替代）**

- **树内**（树图 / 树内搜索 / 树图选人）：**保留镜像本身**，点击按 §9-7-2 / §9-7-3 打开真身档案；管理选人、审批选人列表**一律不解析**（§9-7-8 红线，写路径依赖镜像 handle）。
- **全站搜索**（`GET /search/global`，**既有已实现口径**，不属本轮改动）：对镜像**归并到真身** —— chief 实测搜「季贺为」只返 **1 条**（真身，`restricted:false`）。即「全站搜索按镜像归并」与「树内保留镜像」**分工并存**。
- **分迁占位**：`external_link_type='branch'` **不并入**镜像判据，维持跳转新树（§9-7-9）。
- **跨树选配偶发现通道**（**§9-8**，本轮新增口径 B）：只服务「**跨树选配偶**」这一场景；上两条分工（树内保留镜像 / 全站搜索按镜像归并）**均不受其影响**（§9-8 第 7 条）；本通道属**写路径选人**，沿用 §9-7-8 红线（**保留镜像本身、不解析**）。

**统计栏口径澄清（真机质检裁定 2026-09-19）**

- **统计口径 = 按当前可见森林里「实际绘制出的」镜像节点分档统计** —— 不是按 `tree.people` 里镜像节点的全集统计。
- 因此**某一档为 0 属正常**（UI 上表现为该档不显示，§9-7-7「只显示大于 0 的档」）。最典型 = `marriage` 档：
  `gu_39038_01` 内确有 1 个 `marriage` 镜像（`I000292` 季清昆），但它是「**配偶姻亲根**」，被 `frontend/src/business/pedigree.ts`（**第 108-129 行**「排除配偶姻亲根」）恒排除、**不作为树图节点** ⇒ 该树统计栏**没有「外树配偶」档**。
- `marriage` 档只在「**婚姻镜像本身构成树图节点**」时才可能出现；**档位映射（§9-7-6）与该档是否出现是两件事**，不得据档位映射反推“该树必有外树配偶 1”。
- **真机实测（质检裁定，2026-09-19）**：`gu_39038_01` 树图统计栏 = **`外树子女 2 · 外树始祖 1`**（**无「外树配偶」档**）。
- 本节其余内容与 §10 前端落点**不受此澄清影响**；状态为 **已实施 + 真机质检通过（2026-09-19）**（数值证据见下「真机质检证据」）。

**事实锚点（真源已核对，可作实施 / 质检的实测证据）**

- 真身：`ji_23395_01` `I000159` 季贺为 handle `103f95b87eae32faba04050101ed`；镜像：`gu_39038_01` `I000294` 季贺为 handle `753f8a77dafb9728b0057327`（`external_mirror='true'`、`external_link_type='child'`、`external_tree='ji_23395_01'`）。
- `gu_39038_01` 另有镜像 `I000143`（`founder`，指向祖谱 `gu_39038`）、`I000292`（`marriage`，**配偶姻亲根、不进树图**）、`I000293`（`child`）；该树 `person_count = 23` **含 4 个镜像**（真人 **19**），其中**实际绘制出的镜像 = 3 个**（`child` ×2 + `founder` ×1）⇒ 统计栏 `外树子女 2 · 外树始祖 1`。
- 成因 = **跨树婚姻**（顾景月嫁入季家，双树各留对方镜像）＋「**跨树婚姻的子女归父真身所在树**」（§5 写入矩阵「子女」行 / §7-4，本树只留镜像子女）。

> 实施状态：**已实施 + 真机质检通过（2026-09-19）**，前端落点见 §10。

**真机质检证据（2026-09-19 · 真机质检裁定；数值照抄，不得改写）**

- □ 顾树 `gu_39038_01` 树图 `serie.data` **23 项**（**1 虚拟根 + 22 真人**）、有图形元素的真人项 = **22**、`notDrawn` **空**。
- □ chief 点镜像 `I000294` → 弹窗顶部标注「**本节点为 季氏费县白露家族 000159 的镜像 · 内容取自真身**」、编号行显真身 **000159**；点镜像 `I000293` → 同口径得到 **000149**。
- □ guest 点 `I000143` → 回退本树副本 + 「**真身内容不可见**」、`exceptions=0`。
- □ 祖谱 `clan-hall` 顶端链镜像（姒期视）→ 「**本节点为 中华世本 · 全球华人家谱总谱 0137 的镜像**」。
- □ 统计栏分档实测 = 「**外树子女 2 · 外树始祖 1**」（见上「统计栏口径澄清」，已在上轮更正）。
- □ 非镜像节点**回归无标注、行为不变**。

### 9-7-10 树图渲染完整性（口径 A 的前置修复）

> 本条是**镜像可点、可见**的前置条件：上述「点击开真身」质检若缺本条，会以「镜像不渲染 / 不可点」的形式**误报**。

1. **整树全展开必须传 `initialTreeDepth = -1`** —— 依据 `node_modules/echarts/lib/chart/tree/TreeSeries.js`：**`:79`** 起 `tree.eachNode('preorder', …)` 以 `node.depth` 求 `treeDepth`（ECharts 内包**虚拟根**，故真实树深 = 该值）、**`:90`** `expandTreeDepth = expandAndCollapse && option.initialTreeDepth >= 0 ? option.initialTreeDepth : treeDepth`（**仅 `initialTreeDepth < 0` 时走真实树深**）、**`:94`** `node.isExpand = item && item.collapsed != null ? !item.collapsed : node.depth <= expandTreeDepth`。→ **传正数**会因虚拟根多占一层，令**最深一层不建图形元素、不可点**；前端落点 `frontend/src/components/tree-pedigree/tree-pedigree.vue` 的 `expandDepth`（`props.initialDepth > 0 ? Math.min(...) : -1`）、`initialTreeDepth: expandDepth`。`layerBox`（层级定位）仍用**真实树深**。
2. **整树模式必须 `expandAndCollapse: props.initialDepth > 0`** —— 否则 ECharts（`TreeView.js:169`）会给卡片绑「**点击 = 折叠子树**」，与「**点卡片开档案**」叠加 → **后代静默消失**；**这正是质检将「镜像不渲染」误报的成因**。运行期 A/B 实测：**关 = 点击后 22**；**开 = 点击后 20 且 `notDrawn = [I000293, I000294]`**（两个镜像节点即失踪者）。
3. **深链页必须把地址栏收敛回镜像原坐标** —— `frontend/src/pages/person/detail.vue`（顶部注释 `:35-36`、收敛逻辑 `:52` / `:81` / `:86`）：真身不可见 / 拉取失败**回退本树副本时**，必须将 URL 收敛回**镜像原坐标**，否则**访客刷新即真身 404 错误页**（且分享链接对访客失效）。**变异验证**：移除该行 → reload 后 body **20 字** + 错误文案 + **2×404**。

### 9-7-11 【新口径 v2】家族树树图**不绘制**「外树子女镜像」（Kevin 2026-09-22 拍板 · **取代 §9-7 在「家族树树图绘制」上的语义**）

> **取代标注（历史行一律原文保留、不改写）**：本条**取代** §9-7（口径 A）与 §9-7-7 在「**家族树（`kind === 'family'`，含 `kind` 缺省）树图绘制**」上的语义 —— 即「镜像节点在树内**保持可见** + 镜像计入**按当前可见森林里实际绘制出的镜像节点分档统计**」这一组口径，在**家族树树图**范围内**不再适用**。§9-7 正文（含 §9-7-6 / §9-7-7 与下「**真机质检证据**」段）**原文保留**；其读数系**旧绘制口径**下的记录（例：`gu_39038_01` 统计栏「**外树子女 2 · 外树始祖 1**」），**以本节为准**。
> **不属取代范围（逐条不变）**：§9-7-2（点镜像 → 打开真身档案）/ §9-7-3（顶部镜像标注）/ §9-7-4（按真身树权限判定）/ §9-7-5（真身不可见 → 回退本树副本）/ **§9-7-8（红线：管理选人 / 审批选人列表保留镜像本身、不解析）** / §9-7-9（`external_link_type='branch'` 分迁占位维持跳转新树）/ §9-7-10（树图渲染完整性）。
> **状态**：前端实现 **由 Kong 实施中 / 由 Neng 质检** —— 本节**只登记口径**，**不表示已实现 / 已通过 / 已部署**，**不得**当作实施证据或质检证据引用。

**R1 · 显示口径（新）**

- **家族树**（`tree-meta` 判定 `kind === 'family'`，**含 `kind` 缺省**）的**树图不绘制「外树子女镜像」**。
- **判据（简单判据 · Kevin 2026-09-22 拍板：不看本姓）** = `String(p.external_mirror) === 'true'` **且** `p.external_link_type === 'child'`。
- 该节点**连同其子树整支不绘制**。
- 树类语义**逐字取自** `cloudfunctions/compat-api/lib/family-population.js` **`:35-40`** 的 `treeKind()`：`is_master === true || kind === 'master'` → **世本**；`kind === 'clan'` → **祖谱**；**其余（含缺省）** → **家族树**。

**R2 · 例外（不动）**

- **世本（`zhonghua`）/ 祖谱（`kind === 'clan'`）不适用**本口径：**顶端链镜像**（`chain`）、**始祖镜像**（`founder`）、**祖谱登记镜像**照旧绘制。

**R3 · 数据零写入**

- **不删不改真源节点**；**镜像必须保留** —— 依据 = **§9-7-8 红线**（管理选人 / 审批选人**写路径依赖镜像 handle**）。本批**只改前端绘制**。

**R4 · 后端零改动**

- **不改 `cloudfunctions/compat-api/**`** ⇒ 本批**没有云函数重打包 / `tcb fn deploy` 项**。
- 前端改动**最终需要一次前端发布**（`build:h5` + `tcb hosting deploy` / `build:mp-weixin` 上传），按 `docs/PENDING_DEPLOY.md` 的既有格式登记（本批 → 同册 **§30**）。

**R5 · 回归面（一律不变）**

- 卡片角标档位（§9-7-6）、统计栏、点卡片开真身档案、树内搜索、全站搜索、管理选人列表、多支始祖列表、`hideExternalMarkers` 开关语义 —— **一律不变**。

**事实基线（Zang 2026-09-22 实测取证；数值照抄，不得改写）**

- `shen_27784_01` 的 `I000286` 季清昆（handle `e775ed1f70955cc9f1f9b47a`）= **外树子女镜像**：`external_mirror='true'` + `external_link_type='child'` + `external_tree='ji_23395_01'`；**真身** = `ji_23395_01` 的 `I000254`。
- **现行代码把它当树图卡片绘制**（Zang 用真源 JSON + `frontend/src/business/pedigree.ts` 实跑：`shen` 森林 **1 根 / 9 节点**，含该镜像；`gu_39038_01` 森林 **22 节点**，含 `I000293` / `I000294`）。
- **全站同类节点仅 3 个 / 2 棵树**：`shen_27784_01` 的 `I000286`、`gu_39038_01` 的 `I000293` / `I000294`。
- **真源 md5 基线（Zang 2026-09-22 13:0x 取）**：`migrate-output/trees/shen_27784_01.json` = **`407445ab3a507b1f591faf005dfb6ae2`**；`config/tree-meta.json` = **`59e0271f443d4ae682cc939a064fadff`**。
- **Jing 现盘只读复测（2026-09-22 13:19:20 CST）**：`migrate-output/trees/shen_27784_01.json` = **`407445ab3a507b1f591faf005dfb6ae2`**（**与上基线一致**）；`config/tree-meta.json` = **`5688ce406a59b3ca27b9a22ebd3ddb14`**（**与上基线不同**；`git show HEAD:config/tree-meta.json` = `13616a89db2782256c3f33260aa32470` ⇒ **三值互不相等**，该文件的基线值**未能在现盘 / HEAD 复现**）。现盘差异归属 = 该文件当前为**工作区在途改动**（`git status --short` = ` M`，属 Kevin 自己的在途工作，**本批未触碰**）。⇒ 本批**不据该值做任何部署 / 回归判据**，仅如实登记。

**文档侧登记（Jing · 2026-09-22）**

- `AGENTS.md` **§7** 的「口径指路」行文本**已备**，但**写入被 protected-file 审批拦下**（审批提示超时、未获用户同意 ⇒ `AGENTS.md` **本轮未被修改**）⇒ **待 Kevin 批准后由前台 / Zang 写入**；本节的 R1–R5 口径与状态**不受此影响**。

### 9-8 跨树嫁娶配偶发现通道（口径 A+B · Kevin 2026-09-19 拍板）

> 用户报障：**娶入时搜不到目标家族树中指定的女性节点**（该节点确实存在，搜索却返回 0 条）。
> 状态：**已实施 + 冒烟 / 单测通过（2026-09-19）** —— 后端通道 `GET /search/marriage-candidates` 已落地并冒烟（证据见下），本节口径不变。

**冒烟 / 单测证据（2026-09-19；数值照抄，不得改写）**

- □ 未登录 → **401 `{"error":"登录已过期或未登录"}`**（**不降级 guest**）。
- □ 已登录 member（`13800000001`）搜 `gu_39038_01`「景月」→ **1 条** `I000140 顾景月 F`；搜 `long_40857_01`「扬」→ **1 条** `I000239 龙扬 F`。（**修复前**同身份树内 `/search` 均为 **0** 条，已作为对照。）
- □ 出参键集**恰为** `handle` / `gramps_id` / `name` / `gender` / `is_living` **五项**。
- □ **镜像排除**：`I000292` 查得 **0**；
- □ **性别未知不列入**。
- □ 单测 `cloudfunctions/compat-api/lib/marriage-candidates.test.js` **11/11**，并已注册进 `scripts.test`（根 `package.json`）。
- □ **变异验证**：改回套 `isHiddenPerson` → 5 项变红、共 **`5 pass / 6 fail`**。
- □ 全量 `npm test` = **391 tests / 391 pass / 0 fail** —— ⚠️ **该计数跑于 2026-09-19 12:10**；其后另有**并发工作流**改了 `cloudfunctions/compat-api/lib/tree-write.js` / `lib/clan.js` ⇒ **提交前必须重跑**，本节**不据此断言当前工作区全绿**。
- 前端落点（§9-8 第 4 / 第 5 条）：**已实施 + 真机质检通过（2026-09-19）** —— T4 四项证据（娶入弹窗搜索 / 空态三态文案 / 「按全局编号指定」disabled→enabled / 真实 401 文案）与**管理选人红线对照**见 §10 末「T4 真机质检证据」。

**已核实的缺陷（两层 + 一处不对称 + 一条先例）**

- **第 1 层（结构性）**：读路径节点级分层按「**发起人对目标树的关系**」裁剪 —— `cloudfunctions/compat-api/lib/tree-access.js` 的 `computeAccess`（**约 82-89 行**）只有 **总谱（`is_master`）/ `chief_editor` / 目标树成员（`anchorTreeId === treeId`）** 判 `full`，其余按 `hideTail` 裁剪（登录 **9** 世 / 未登录 **18** 世；兜底 `FLOOR_VISIBLE_DEPTH = 1`）。`GET /search` 对每个命中都套 `isHiddenPerson` 裁剪（`index.js:2479`、`2484`）。
  实测：`gu_39038_01`（4 世 / 23 人）搜「景月」→ guest **0** 条、季树 member **0** 条、chief **1** 条（`I000140` 顾景月 F）；`long_40857_01`（2 世）搜「扬」→ guest / member **0** 条、chief **1** 条（`I000239` 龙扬 F）。前端娶入 / 嫁出弹窗的搜索走 `searchPeople`（`frontend/src/components/person-archive/person-archive.vue` 约 **2124-2134 行**）→ 树内 `GET /search`；弹窗**不区分**「确实无此人」与「被权限隐藏」。
- **第 2 层（放大器）**：`lib/auth.js` 的 `authUser`（**49-58 行**）在 token 失效 / 过期时 `verifyJwt` 返 `null` → **静默返 `null` 不报 401** → 服务端按 **guest 档（藏 18 世）** 处理，而前端仍显示 chief（`role` 只读 localStorage）→ 用户**完全看不出**自己已被当访客裁剪。
- **不对称真相**：写侧 `/marriage-request`（`index.js:1121-1128`）用 `resolveNode(spouse_ref, '', {target_tree_id})` 解析对方节点，**根本没有可见性校验**（即权限口径 C：目标树不设门槛，靠申请-审批把关）→ 形成「**发现被拦、写许可开着**」。
- **已有限制先例（同类裁决可引用）**：加入申请的**支系线索校验**（`index.js:908-911`）是**有意收紧**并给**明确提示文案**。

**八条口径（Kevin 2026-09-19 已拍板，逐条写死、不得增删数值）**

| # | 口径 |
|---|---|
| 1 | **新增专用发现通道** `GET /search/marriage-candidates`（参数 `query` / `tree_id` / `gender` 可选）：**需登录**（未登录 / token 失效 → **401 明确错误**，**绝不静默降级为 guest 档**）；**不套节点级读裁剪**（**不调用** `isHiddenPerson` 过滤）；**注册位置在树编辑闸门之前**。 |
| 2 | **返回字段白名单（硬性）**：`handle` / `gramps_id` / `name` / `gender` / `is_living` **五项**；**不得**返回生卒、详情、家族关系、`external_*` 指针、真身 handle / 编号 / 树。理由：口径 C 靠「申请-审批」把关，**发现环节只需能选人**。 |
| 3 | **性别口径不变**：娶入仍**强制目标为女性**（`gender === 'F'`）、嫁出仍**强制男性**（`gender === 'M'`）；**性别未知（`U` / `0`）不得列入**（用户明确不改）。前端现有 `filter(p => p.gender === want)` **保留**。 |
| 4 | 前端娶入 / 嫁出弹窗**改调**上述通道；**无结果时的文案必须区分三种情形**：① 未登录 / 登录过期（**401**）② **通道错误** ③ **确实无匹配**。 |
| 5 | 同时提供「**按全局编号指定**」输入框作**兜底**：后端口已支持 `spouse_ref` / `spouse_handle` 的**全局编号解析**（`index.js:1121-1128`）；**编号优先、可与搜索共存**；编号解析失败的错误文案**沿用后端现有文案**（`找不到编号/句柄为「…」的配偶节点`）。 |
| 6 | **列表上限沿用 `/search` 的 20**（可配）；**空 `query` 行为与 `/search` 一致（返空数组）**。 |
| 7 | **与既有口径的关系**：`GET /search/global` 的**镜像归并**口径、树内 `/search` **保留镜像**的分工**不受本通道影响**（本通道只服务「跨树选配偶」这一场景）；`docs/permission-tier.spec.md` 的读分层对**列表 / 树图 / 搜索**的默认行为**不变**，本通道是**白名单字段下的定点例外**。 |
| 8 | **反例登记（已知不修）**：首页 / 列表 / 树图**仍按分层裁剪**；加入申请的**支系线索仍收紧**（本次不动）。 |

**实现落点与红线**

- **路由注册位置（硬性）**：必须注册在**树编辑闸门之前** —— 闸门 = `index.js` 的 `if (!treeId) return send(400, { error: '缺少 X-Tree-Id' })`（**第 2331 行**）；同段先例 = `GET /search/global`（**第 2096 行**起注册，注释见第 **2085-2086 行**「不需要 X-Tree-Id，且必须注册在树编辑闸门之前」）。本通道的 `tree_id` 走**查询参数**（闸门之前拿不到 `X-Tree-Id` 语义）。
- **鉴权（与写侧同档）**：唯一放行条件 = 「有效 JWT **且** `jiazu_users` 中存在该用户」（`authUser` 返非 `null`）；**token 缺失 / 失效 / 过期一律 401**，文案沿用后端既有 401 文案（`请先登录后再进行编辑操作`），**不得**在 `authUser` 返 `null` 时静默回落 guest 档 —— 该静默回落正是第 2 层缺陷的放大器。
- **不裁剪的实现口径**：本路由**不调用** `computeAccess(...).isHiddenPerson`；阈值 `GUEST_HIDE_TAIL = 18` / `LOGIN_HIDE_TAIL = 9` / `FLOOR_VISIBLE_DEPTH = 1` **三处数值不变**，仅是**本路由不受其约束**。
- **字段白名单口径**：候选项按 `handle` / `gramps_id` / `name` / `gender` / `is_living` **五键显式白名单**输出（**不得**透传 `toRawPerson` 全量对象）。
- **与镜像的关系（沿用既有红线，不新增口径）**：本通道属**写路径选人**（选出的 handle 直接进 `/marriage-request`）→ 沿用 **§9-7-8 红线**：**保留镜像本身、不做解析到真身**；这与「全站搜索按镜像归并」（§9-7）分属两条路径，互不影响（§9-8 第 7 条）。候选恰为镜像时的处理见 **§11-5（登记项，本轮不改）**。
- **前端落点**：`frontend/src/business/api.ts`（在 `searchPeople`（**约 1680 行**）旁新增走本通道的查询函数）+ `person-archive.vue`：`doSearchMarry`（**第 2115-2138 行**）改调；空态（**第 452 行**，现为单一句「未找到…节点」）按第 4 条**拆三态**；弹窗新增「按全局编号指定」输入框（编号优先、与搜索共存）。
- **反例（不得借本通道顺手改）**：`docs/permission-tier.spec.md` §2 / §3 的档位与阈值、`GET /search`、`GET /search/global`、`GET /people/*`、`GET /families/*`、`GET /tree/rank` 的裁剪行为**一律不动**；加入申请的支系线索校验**不动**。

### 9-9 展示层：家族树共享选择器（tree-picker 口径 · Kevin 2026-09-19 拍板）

> 状态：**已实施 + 真机质检通过（2026-09-19）**。触发问题 = 嫁出 / 娶入弹窗把候选家族树平铺成纵向胶囊列表，**把弹窗撑开**（`.modal` 原本 = `max-height:85vh` + `display:flex` 且**无 overflow**）。
> 同一形态的平铺块**全仓共三处**（嫁出·娶入 / 认祖选祖谱 / 挂载祖谱）——报障只提了一处，本批**一次收口三处**。

**组件与接入点**

| # | 项 | 口径 |
|---|---|---|
| 1 | 组件 | 新建 `frontend/src/components/tree-picker/tree-picker.vue`（**共享组件，不做弹窗内联块**）；props `items: {tree_id,label,sub?,group?}[]` / `modelValue` / `placeholder`，emit `update:modelValue` + `select`。 |
| 2 | 接入点 | `frontend/src/components/person-archive/person-archive.vue` **三处**：**嫁出 / 娶入**（选择目标家族树）、**认祖**（选择认祖目标）、**挂载祖谱**（选择要挂载的祖谱）。 |

**展示层与交互口径（冻结）**

1. **折叠态恒为一行** —— 实测高 **44px**，**不随候选数量变化**；这是「弹窗不再被撑开」的口径保证（原平铺 `.tree-opt` 列表 14 棵约 **670px**）。全仓 `grep -rn 'tree-opt' frontend/src --include=*.vue` 现为 **0 命中**（旧平铺块已整体删除，不留兼容分支）。
2. **展开态 = 独立覆盖层**，`z-index: 1200`（**高于既有 `.modal-mask` 的 999** ⇒ 展开时不会被任何既有弹窗遮罩压住）；内部三段 = **搜索框**（本地过滤，**大小写不敏感**，匹配 `label` / `sub` / `tree_id`）+ **列表区 `max-height: 36vh` 且内部滚动** + **底部取消 / 确定**。
3. **分组名由 `treeKindLabel(kind)` 推导**（`frontend/src/business/api.ts:1294`）：`clan` → 祖谱 / `master` → 中华世本 / **缺省** → 家族树；**仅当实际存在 2 个以上分组时才渲染分组小标题**（单分组平铺、不显标题）。
4. **列表项** = `label`（单行省略）+ 可选 `sub` 小字（单行省略，如挂载祖谱的「自有 N 人 · 已认祖世本」）+ **右侧 `tree_id`**；**`sub` 与右侧 `tree_id` 不得重复**（嫁出·娶入与认祖的 `sub` 不传 —— tree_id 已由右侧直显属冗余）。
5. **搜索无结果 → 「未找到匹配的家族树」**（与 §9-8 第 4 条的**配偶空态三态文案**是两回事，互不覆盖）。
6. **交互（草稿语义）**：选择层内为**草稿**；**取消 / 点遮罩 / ✕ 均只关覆盖层、不改动已选值**；**只有「确定」才回填**（emit `update:modelValue` + `select`）。
7. **弹窗容器兜底同批补齐**：`.modal` 补 `overflow-y:auto` + `margin:auto`、`.modal-mask` 补 `overflow-y:auto` + `padding:5vh 0`（既保居中、又保证超高时顶部不被裁）——已落地在 `person-archive.vue:2350-2359`，否则下一个长内容还会外溢。

**不得改变的红线（本批逐条冻结）**

- **候选集合不得改变**：嫁出 / 娶入 仍为「**非 `is_master` 且非本树**」，候选不因换壳而增减；认祖 / 挂载的候选来源与 §9-8 / §10 原有路径一致。
- **冻结文案一字不变**：空态三态（「**未找到符合条件的女性节点 / 未找到符合条件的男性节点 / 登录已过期，请重新登录**」）、**后端错误原文**、「**按全局编号指定（兜底，如 000052 / I000052）**」label 及其 placeholder、**初娶 / 再娶（第 N 次）**序号文案、**身份确认块**、**提交按钮禁用条件** —— 一律只换外壳，**不动文案与语义**。

**真机质检证据（2026-09-19 · 真机质检裁定；数值照抄，不得改写）**

- □ 娶入弹窗折叠态 **44px**（一行）；旧平铺列表 `.tree-opt` 数量 **0**。
- □ 弹窗几何 **763.75px ≤ 0.86 × 视口** ⇒ **不再被撑开**。
- □ 列表区 **504px = 36vh** 且**内部滚动**（弹窗与页面不跟着滚）。
- □ 分组 **2 个**；挂载祖谱候选全同组 ⇒ **只 1 组时不渲染小标题**。
- □ 搜索「顾」→ **收敛 1 项**；搜「zzz」→ 空态「未找到匹配的家族树」。
- □ 选中 → 点**确定** → **回填成功**。
- □ 嫁出侧：标题 / 标签 / 空态「未找到符合条件的男性节点」/ **初嫁** 文案均不变。
- □ 假 token → **「登录已过期，请重新登录」**（三态第 ① 态）。
- □ **嵌套**：弹窗内展开选择器时，点**内层遮罩只关内层**；点**弹窗内部不关**（§9-10 成对断言的 `@click.stop` 侧）。

**已知边界（登记项 —— 不当缺陷、本批不提修法）**

1. **嵌套两层时在同一坐标快速连点两下**：第一下关内层、**第二下落在外层遮罩上把外层也关掉** —— 这是**两次独立点击**（**非事件穿透**）；**不追加时间戳守卫**（守卫会引入新的时序语义，收益不足）。
2. **`pages/market/index.vue`「购买确认」弹窗**因当前**无在售挂单**、无法真机触达 —— 其遮罩改动与**已验证的 4 处同构**，且 `vue-tsc --noEmit` **exit 0**。
3. **认祖选择器**因真实数据**全库无 `founder_state='none'`**（始祖均已挂载）而无法真机触达。

---

### 9-10 【跨模块通用坑】uni-app H5 下 `@click.self` 不生效 —— 遮罩关闭必须「遮罩 `@click` + 面板 `@click.stop`」**成对写**

> 本节**不属嫁娶专有口径**：它是**全仓所有 vue 页面的通用坑**，登记于本文件只因本批落点与嫁娶弹窗同批。后人在**任何页面**新增「点遮罩关闭」弹窗时**先读本节**。

- **现象**：uni-app H5 下 `<view>` 上的 **`@click.self` 修饰符不生效** —— 实测**真鼠标事件 / `el.click()` / 合成 `MouseEvent` 三种均不触发** ⇒ 全仓所有「点遮罩关闭弹窗」**原本全部失效**（用户体感：「点了外面没反应」）。
- **可靠写法（两者必须同时改，缺一即错）**：
  1. **遮罩层**：`<view class="modal-mask" @click="原 handler">`（**去掉 `.self`**）
  2. **紧邻的内层面板容器**：`<view class="modal" @click.stop>`
  - ⚠️ **只改遮罩、不改面板 = 「点弹窗内部任意处即关闭」的重大回归**（`.stop` 是唯一的内部点击拦截）。验收必须**成对断言**：**点遮罩 = 关**、**点面板内部 = 不关**。
- **本批修正清单（全仓 11 处，`@click.self` 全部替换为上述成对写法）**：

| # | 文件 | 处数 | 弹窗 |
|---|---|---|---|
| 1 | `frontend/src/components/person-archive/person-archive.vue` | **6** | 嫁出 / 娶入、删除节点、汇宗、认祖、挂载祖谱、绝婚 / 合离 |
| 2 | `frontend/src/components/clan-hall/clan-hall.vue` | **1** | 编辑祖谱信息 |
| 3 | `frontend/src/pages/hall/index.vue` | **2** | 加入申请、建谱申请 |
| 4 | `frontend/src/pages/mine/index.vue` | **1** | 解绑申请 |
| 5 | `frontend/src/pages/market/index.vue` | **1** | 购买确认 |

- **全仓自查（回归判据）**：`grep -rn '@click.self' frontend/src --include=*.vue` 必须 **0 命中**（本批实测 **0**）。
- **同写法（同构、口径一致）**：`pages/index/index.vue:139-140`（`.ct-mask @click="closeCreate"` + `.ct-modal @click.stop`）、`person-detail-modal.vue:3` 内层 `@click.stop`、`tree-picker.vue:11`（`.tp-panel @click.stop`）—— 新增弹窗照此**对写**即可。
- 与 §9-9 第 7 条同属「弹窗不再被撑开 / 点得掉」的一组修复：**换壳与遮罩必须成对收口**，只做一半会得到「点不掉的弹窗」或「点内部就关的弹窗」。

## 10. 实施拆解（本文件确认后执行）

- **后端 compat-api**
  - `lib/marriage.js`（纯函数，全部单测）：`nextMarriageNo`、`marriageLabel(no, direction)`、`mirrorPerson(src, fromTreeId, mid)`、`planMarriage(treeA, treeB, ...)`、`planEnd(treeA, treeB, kind)`、`normalizeMarriageDate`、`dissolveKind` 守卫。
  - `store.js`：跨树写入 `updateTrees([a, b], fn)`（内存内先整体应用 → 顺序持久化 → 失败回滚已写的一侧）+ `colGet/colSet('jiazu_marriage_requests')`。
  - 路由：§8 表（`/marriage-request`、`/admin/approve-marriage`、`/admin/reject-marriage`、`/admin/marriage-requests`、`/admin/marry-end`）。
  - **`index.js`：新增发现通道 `GET /search/marriage-candidates`（§9-8 八条）** —— **注册在树编辑闸门之前**（闸门 `缺少 X-Tree-Id` 在**第 2331 行**；参照同段 `GET /search/global` 第 **2096** 行）；**需登录**（无有效 JWT / token 失效 → **401**，**不得**回落 guest 档）；**不调用** `isHiddenPerson`；入参 `query` / `tree_id` / `gender`；出参**五键白名单**（`handle` / `gramps_id` / `name` / `gender` / `is_living`，**不得**透传 `toRawPerson` 全量对象）；上限 **20**（可配）、空 `query` → **`200 []`**；性别未知（`U` / `0`）**不列入**。**已实施 + 冒烟通过（2026-09-19）**：未登录 **401**、member 搜「景月」**1 条**、键集**恰五键**、镜像 `I000292` 查得 **0**（见 §9-8 冒烟 / 单测证据）
  - 单测（新增，建议 `lib/marriage-candidates.test.js` 同级纯函数 / 路由级）：**401 不降级**（无 token / 失效 token / 过期 token 三例）、**出参键集恰为五键**（无生卒、无 `external_*`、无真身信息）、性别过滤（`F` / `M` / `U` 三例）、上限 20 与空 `query` 返 `[]`、闸门之前注册（无 `X-Tree-Id` 不返 400）。**已实施（单测 11/11 + 变异验证 5 pass / 6 fail，2026-09-19）**
- **前端**
  - `person-archive.vue`：入口「💍 嫁出」/「🤵 娶入」（选目标树 → 搜索对方 → 选填日期 → 提交申请，提示"已提交，等待对方家族审批"）；配偶区显示「配 <名>（外树 <树>）· 初嫁/再嫁 · 成婚 1957」+ 结束信息；「⚔️ 绝婚」/「🤝 合离」入口（选填日期 + 说明）。
  - `pages/admin/index.vue`：新增「联姻 / 离异申请」审批区（与「加入申请」同构）。
  - `tree-pedigree.vue`：镜像节点**卡片角标按 `external_link_type` 分档**（`marriage` 外树配偶 / `child` 外树子女 / `founder` 外树始祖 / `chain` 外树上层链 / 其它 外树登记，§9-7-6）；**统计栏按档分列**（只显示 > 0 的档，§9-7-7）、不再用「含外树配偶 M」；marker 过滤按 §9-3 收紧（**镜像保持可见**、隐藏语义只留非镜像的 `founder` 登记标记，§9-7-1）。**已实施 + 真机质检通过（2026-09-19）**（**已作废 2026-09-19**：卡片不再单列镜像，人数口径见 docs/home-sort-search.spec.md §10-6；`mirror_count` 退为保留字段、无消费者）
  - `business/cross-tree.ts`：新增**纯函数** `mirrorTargetOf`（镜像 → `{ treeId: external_tree, handle: external_person_handle }`，判据 §9-7-1、目标按 §9-7-2）与 `mirrorLabelOf`（档案顶部标注与卡片角标文案；档位文案见 §9-7-6、标注文案见 §9-7-3、树名取 `tree-meta` `display_title` 取不到回退 `tree_id`）。**已实施 + 真机质检通过（2026-09-19）**
  - `person-archive.vue`：新增**顶部镜像标注 prop**（「本节点为 (真身树 `display_title`) (真身全局编号) 的镜像 · 内容取自真身」，§9-7-3）；打开镜像即按 §9-7-2 加载真身档案、**编辑与谱系管理按真身树权限判定**（§9-7-4）、真身不可见 / 拉取失败 → 回退镜像本树副本并提示「真身内容不可见」（§9-7-5）；管理选人 / 审批选人列表**保留镜像本身不解析**（§9-7-8 红线）。**已实施 + 真机质检通过（2026-09-19）**
  - `api.ts` / `business/index.ts` / `types.ts`：新接口与 `external_marriage_*` 透传。
  - `business/api.ts`：在 `searchPeople`（**约 1680 行**）旁**新增**发现通道查询函数（如 `searchMarriageCandidates`，走 `GET /search/marriage-candidates`；不套 `tree_id` header 依赖）；`business/index.ts` 一并导出。**已实施 + 真机质检通过（2026-09-19）**（代码落点：`business/api.ts:1730` `searchMarriageCandidates` 已导出；真机证据见本节末「T4 真机质检证据」）
  - `person-archive.vue`（娶入 / 嫁出弹窗，§9-8 第 4 / 第 5 条）：`doSearchMarry`（**第 2115-2138 行**）**改调新通道**；空态（**第 452 行**）由单一句「未找到…节点」**拆三态**（① 未登录 / 登录过期（401）② 通道错误 ③ 确实无匹配）；**新增「按全局编号指定」输入框**作兜底（**编号优先、可与搜索共存**；编号解析失败文案**沿用后端现有文案** `找不到编号/句柄为「…」的配偶节点`）；`filter(p => p.gender === want)`（**第 2132-2133 行**）**保留**（§9-8 第 3 条）。**已实施 + 真机质检通过（2026-09-19）**（代码落点：`doSearchMarry` 第 **2145-2175** 行已改调新通道、401 文案「登录已过期，请重新登录」；空态三态与「按全局编号指定」兜底同在；真机四项证据见本节末「T4 真机质检证据」）
  - `api.ts` 的 `marriageRequest` 入参类型（**第 1123-1132 行**）当前**仅**含 `spouse_handle` / `spouse_tree_id`：编号优先可**复用 `spouse_handle` 传全局编号**（后端 `resolveNode` 已按「全局编号 / handle / 树内旧号」解析），或**补 `spouse_ref` 字段**（后端 `body.spouse_handle || body.spouse_ref` 两者皆认）—— **两种写法均不违反 §9-8 第 5 条**，由 Kong 择一。**已实施 + 真机质检通过（2026-09-19）**（写法 = 「**补 `spouse_ref` 字段**」：`business/api.ts:1129-1134` 新增 `spouse_ref?`（注释「与 `spouse_handle` 二者皆认、编号优先」），`person-archive.vue:2187-2196` `doMarry` 填编号时只传 `spouse_ref`、不传 `spouse_tree_id`；真机证据 = 下方 T4 第 ③ 项）。
  - **T4 真机质检证据（2026-09-19 · 真机质检裁定；数值照抄，不得改写）**：**娶入弹窗四项** —— ① 龙树搜「扬」→ **龙扬 / 000239 / 女**；② 空态文案 = 「**未找到符合条件的女性节点**」；③ 填 `000140` → 确认文案**含 000140**、按钮 **disabled → enabled**；④ **真实 401** → 文案「**登录已过期，请重新登录**」；**全程非 GET 请求 = 0**。**管理选人（§9-7-8 红线对照）**：（加子 → 从树中选择）搜「清昆」→ 候选 `季清昆 / **000292**`（**未被解析成真身**、**无 000254**）。
  - **`components/tree-picker/tree-picker.vue`（本批新增共享组件）+ `person-archive.vue` 三处接入（嫁出 / 娶入、认祖、挂载祖谱）**：弹窗内长列表收成「触发条 + 独立选择层」（展示层 / 交互 / 红线 / 真机证据见 **§9-9**）；同批把全仓 **11 处**「点遮罩关闭」由 `@click.self` 改为**遮罩 `@click` + 内层面板 `@click.stop` 成对写法**（清单与自查判据见 **§9-10**，全仓 `grep '@click.self'` = **0**）。**已实施 + 真机质检通过（2026-09-19）**
- **验证**
  - 单测：序号/标签、镜像字段、日期规范化、成对性、结束幂等、方向与性别守卫。
  - E2E（数据副本 3450，两棵测试树）：申请→审批→双侧写入 / 驳回不改数据 / 绝婚单方生效 / 合离需确认 / 再嫁序号 / 重复配对拒绝 / 日期选填与非法格式 / **真源 md5 未变**。
  - UI 实测：副本后端跑通"申请 → 审批 → 档案显示配对与日期"。
  - **发现通道（§9-8）E2E**：`gu_39038_01` 搜「景月」→ 登录用户经新通道得 **1 条**（`I000140` 顾景月 F），而树内 `GET /search` **仍 0 条**（分层不动）；`long_40857_01` 搜「扬」→ 新通道 **1 条**（`I000239` 龙扬 F）；**未登录 / 失效 token → 401**（不得 `200 []`）；出参键集**恰为五键**；`gender=U` 候选**不列入**；空 `query` → `[]`；**`GET /search/global` 镜像归并不变**（搜「季贺为」仍 **1** 条）。**已冒烟 / 真机质检（2026-09-19：后端通道实测数值见 §9-8「冒烟 / 单测证据」；前端三态文案与编号兜底的 T4 四项证据见本节末「T4 真机质检证据」）**
- **文档**：README「树内操作」+ `docs/data-model.md` §4 字段表 + 本文件状态改「已实施」+ `docs/PENDING_DEPLOY.md` 补本批路由与集合。**（本轮回写范围：本文件 §9-4 / §9-7 / §9-8 / §10 / §11 —— 已按 2026-09-19 真机质检 / 冒烟证据回写为「已实施」；`docs/PENDING_DEPLOY.md` 的 **§18-1 / §19** 已补真实证据、**§21** 为本批「首页卡片人数单列镜像」新增节。）**

## 11. 仍待确认（不阻塞实施，按草案执行）

1. **绝婚是否也走"目标树审批"**：草案按"男方侧单方生效 + 通知"（与"男方主动解除"语义一致）；若你要求绝婚也需对方确认，改一处校验即可。
2. **合离的确认方式**：草案按"任一侧发起 → 对方审批通过即生效"（一步确认）；若要"两侧各自点同意"，加一个 `confirmed_by_both` 状态即可。
3. 四动作的日期是否要显示在树图卡片上（草案：只在档案婚姻块显示，卡片保持「配XX」）。
4. **首页卡片人数单列镜像**（登记项 → **已实施 + 冒烟（2026-09-19）**）：`GET /tree/rank` **新增出参字段 `mirror_count`**（本树 `people` 中 `external_mirror === 'true'` 的节点数），首页家族 / 祖谱卡片按「**N 人（含外树 M）**」显示（实现 = `frontend/src/pages/index/index.vue` 的 `peopleText()`：`mirror_count > 0` → 「N 人（含外树 M）」；缺失 / 非数值 / ≤ 0 → 只显「N 人」；`person_count` 缺失 → 「— 人」）。**`person_count` 口径不变（仍含镜像）**：实测 `gu_39038_01` = **23** 含 4 个镜像 → 真人 **19**。**`mirror_count` 只用于展示，排序归一化仍用 `person_count`** —— 首页综合分 `0.4×norm(人数) + 0.4×norm(活跃度) + 0.2×norm(新近度)` 与 tie-break 规则**逐字不变**（`docs/home-sort-search.spec.md`；`TreeRankInfo.mirror_count` 类型注释亦明写「**不参与任何排序归一化**」）。（**已作废 2026-09-19**：卡片不再单列镜像，人数口径见 docs/home-sort-search.spec.md §10-6；`mirror_count` 退为保留字段、无消费者）
   冒烟实测（2026-09-19，数值照抄）：`gu_39038_01` **23 / 4**、`ji_23395_01` **89 / 3**、`long_40857_01` **2 / 0**（卡片只显「**2 人**」）、祖谱 `ji_23395` **2 / 1**；单测 `cloudfunctions/compat-api/lib/mirror-count.test.js` **7/7**（**已注册进根 `package.json` 的 `scripts.test`**），与既有 `home-sort-search.test.js` **合跑 30/30**。上云动作见 `docs/PENDING_DEPLOY.md` **§21**（云函数**必须重打包**：出参新增字段）。树图统计栏的分档改动（§9-7-7）不受此项影响。
5. **发现通道的镜像候选**（登记项，**本轮不改**）：§9-8 第 2 条的字段白名单**不含 `external_*`** ⇒ 前端**无法辨别**候选是否为镜像；当目标树中的男性 / 女性候选**恰为该树的镜像节点**时（如 `gu_39038_01` 的 `I000292`），能否直接作为配偶对象（写路径按**镜像 handle** 落单、真身在第三棵树）**另需拍板**。本批按 §9-8 第 7 / 第 8 条执行（写路径**不解析**，§9-7-8 红线），**不阻塞实施**。
6. **后端口 `/marriage-request` 未拦截镜像 handle**（登记项，**本轮未修 · 下一轮**）：写侧按 `spouse_ref` 全局编号 / handle 解析（`cloudfunctions/compat-api/index.js` 第 **1121-1128** 行 `resolveNode(spouse_ref, '', { target_tree_id })`）**不校验解析结果是否为镜像节点**（判据 §9-7-1 三条齐备）。§9-8 发现通道已把镜像**排除在候选之外**（实测 `I000292` 查得 **0**），但**直接按编号调用后端口仍可落单到镜像 handle**（真身在第三棵树）⇒ 需下一轮在写侧补镜像拦截或显式给出放行口径。**未修，不声称已修。**
7. **首页卡片人数单列镜像**（**已实施 + 冒烟（2026-09-19）** = 既有 **§11-4**，此处只做交叉引用）：见 §11-4 与 `docs/PENDING_DEPLOY.md` **§21**；**本条不再挂账**。（**已作废 2026-09-19**：卡片不再单列镜像，人数口径见 docs/home-sort-search.spec.md §10-6；`mirror_count` 退为保留字段、无消费者）

8. **命名说明（主代理裁定：不做重命名）**：`TreeRankInfo.mirror_count`（**全树** `external_mirror === 'true'` **节点数**，用于首页卡片「N 人（含外树 M）」）与 `ClanInfo.mirror_count`（**祖谱顶端镜像段**节点数）**同名字段、语义不同** —— 属两个类型各自的**既有命名**，**不重命名**；**后续新增字段一律避免复用 `mirror_count`**（新口径另起新名，避免第三次同形歧义）。（**已作废 2026-09-19**：卡片不再单列镜像，人数口径见 docs/home-sort-search.spec.md §10-6；`mirror_count` 退为保留字段、无消费者）
